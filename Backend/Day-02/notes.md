first command to run is : 
(npm init -y).


second command to run is : 
(npm i express).


the command that enables us to refresh server on every update is : 
(npx nodemon filename).

how to deploy 

create an file name .gitignore
add node modules and .env in it

